# Design Decisions Reached During Grilling

Follow-up on the 2026-09-01 admin UI session (remove Rate-limit window, add runtime config editor, widen request-detail drawer, show per-section token estimates). Only the config editor decision cleared the ADR bar (hard to reverse, surprising, real trade-off); the other three items are small, reversible UI tweaks tracked directly in implementation, not as ADRs. This file is the ground truth pointer list for critic.

---

- docs/adr/0005-runtime-config-editor.md
